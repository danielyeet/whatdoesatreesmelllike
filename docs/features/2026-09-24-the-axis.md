# The Houses view as a helix round a central axis

Date: 2026-09-24

Files touched: `contact-sheet.js` (rewritten, ~735 lines; ~1,000 since the spokes), the `sheet-*` rules in
`style.css` (the hang's replaced by the axis's; the motifs' canvas raised from z-index 4
to 5), `tests/contact-sheet.spec.js` (the hang's tests replaced).

## What changed

The Houses view of Scent descriptions is no longer a gallery wall. The nine houses stand
on a **helix** of particles winding round a **central axis** down the middle of the page:
one house at the **front**, standing on the axis, large and sharp, with its line about the
house under it; the ones before and after it turned away round the axis above and below,
smaller and fainter the further round they are. You **travel** along the helix — by the
wheel, by dragging, by the arrow keys, by the house numbers standing on the axis, and by
the two buttons at the side with where you are between them. The page arrives with the
axis drawn out from the middle and the houses coming out of it one after another.

## The brief

> I also want you to redisign the houses part again. I want it to be something to do with
> aprticles. give it a central axis too; and make it look impressive, and navigatable.

The hang lasted half a day; [its report](2026-09-24-the-hang.md) keeps why it was built
the way it was.

## Why it is built this way

### The helix

- **A house `d` houses from the front** stands `SPAN` of the page's height below it and
  `TURN` radians (62°) further round the axis (`spot()`): so the next house is turned away
  to the right and down, the one after it nearly behind the axis, and the ones before it go
  round the other way above. How near the eye it is (the cosine of its turn) decides how
  big it is drawn (down to `SMALLEST` right behind the axis), how faint, and whether it
  stands in front of the particles or behind them.
- **The front house stands on the axis**, and only it carries its line about the house
  (the **say**), which used to appear only while pointed at. A ring of specks turns round
  it (**the halo**).
- **Depth is z-index, three layers**: houses turned away behind the axis at 1, the
  particles' canvas at 2, the houses in front at 3 and the front house at 4. So the axis
  and the strands pass behind the house you are on and in front of the ones turned away.
  The houses behind are also softened and greyed a little (`.behind`). The motifs' canvas
  went from 4 to 5 to stay above all of them, and a house being rested on (`.hot`) is 6 —
  with `!important`, because the script writes every house's z-index inline.
- **Every house is the same size at the front** (`FRONT`, `SHAPE`), a picture cropped to
  fill it. The salon hang's many sizes went with it; on a helix the sizes come from depth.
- **On a phone the houses stand further apart** (`SPAN_NARROW`): the helix has no width
  there to turn the next house away in, and it stood on the front one. Only the edges of
  the houses before and after are seen now, top and bottom. Keyed to the width, as every
  phone fix on this site is.

### The particles

All on one canvas (`.sheet-field`), in the page's own ink on the white squared ground:

- **The axis** — a firm line down the middle with a soft **light** either side of it,
  **pulses** running down it, specks always **falling** down it and gathered close about
  it, and ticks every quarter of a house that **travel as you do**, a long one at each
  house, so moving along the helix reads as moving along a ruler. (It was a hairline until
  the owner asked for it emphasised; see below.)
- **The strands** — two, either side of the axis, winding through where the houses stand,
  drawn nearer and heavier where they come towards the eye; bright specks **ride** them.
- **The tethers** — a line of specks from the axis out to every house, with a pulse
  running outward along it.
- **The dust** — a column of specks turning slowly round the axis at their own heights and
  reaches (`DUST`, fewer on a phone), **turning with the helix as you travel**, which is
  what makes travelling read as the whole thing turning rather than the pictures sliding.
- **The corners** — four brackets of specks just outside the front house and its label —
  and **bursts** of specks thrown out as each house arrives. (There was a **halo**, a ring
  of specks turning round the front house; see below for why it went.)
- Specks near the pointer are drawn plainer.

The canvas runs only while the Houses view is on the page (the loop stops when the other
view is showing and is woken when it comes back), and draws at the lower ratio below
700px. With reduced motion nothing falls, turns or rides: it is drawn still and redrawn
when you travel.

### Travelling — the owner's "navigatable"

- **The wheel** moves along the helix, not down the page (`WHEEL`), and it comes to rest
  on the nearest house once the wheel stops (`SNAP_MS`). The page itself does not scroll:
  the houses have the window below the chrome, and no more.
- **A drag** — a finger on a phone, the mouse on a desktop — carries the helix with the
  hand, and comes to rest on the nearest house when let go. A press that was the end of a
  drag opens nothing. `touch-action: none` on the helix so a finger on it drags rather than
  scrolling the page.
- **The keys**: up and down (and left and right, and Page Up and Down) step one house;
  Home and End go to the ends. Tabbing to a house brings it to the front.
- **The numbers on the axis** — one per house, standing on the axis where that house is,
  travelling with it — are buttons that bring that house to the front.
- **The way round** (`.sheet-nav`) at the side: a button up, where you are (*04 / 09* and
  the house's name), a button down. Across the foot on a phone, on the chrome's ground.
- **Pressing a house at the side** brings it round to the front; **pressing the front
  one** steps the page back and opens it, as before.
- **The search** brings the first house it finds round to the front.

### The arrival

The axis is drawn out from the middle of the page (`AXIS_MS`), and the houses come out of
it one after another, **nearest the front first** (`EMERGE_STEP`), each travelling out
along its tether from the axis and fading up on a burst of specks. The front house only
fades up and grows very slightly where it stands: the test that the page never flashes its
own contents also holds that the first house is first drawn where it settles, and it must
not be seen to slide in. The chrome arrives once every house is out, as it did.

### Kept as it was

Resting on a house brings its motifs after a fifth of a second and blurs the rest
(`HOVER_WAIT_MS`), leaving lets them fade, pressing steps the page back before opening,
the number never written into the caption (the search reads the caption), the `js-coming`
hold, the page's own search, and the plain grid of links when the script is blocked.

## How to test it

```bash
npm test -- tests/contact-sheet.spec.js
```

Twenty-two tests. The axis's own, each proved against the fault it guards:

- **`the houses stand on a helix round a central axis, one at the front`** — one house at
  the front, the first, standing on the axis and the largest; the next off the axis, below
  and behind it; the nine numbers on the axis in order; and a column of ink down the middle
  of the canvas. Fails with the front house moved off the axis.
- **`the wheel, the keys, the numbers, the buttons and a drag all travel along the helix`**
  — and the page itself never scrolls. Fails with the wheel ignored.
- **`pressing a house at the side brings it to the front`** — fails when a press at the
  side opens the house.
- **`the houses come out of the axis one after another, nearest first`** — fails with
  every house arriving at once.
- **`the particles move on their own`** — fails with the particles' clock stopped.
- **`the front house and the way round stay inside the window at every width`** — at four
  widths down to a phone, with no sideways scroll and the way round never printed over the
  front house. Fails with the way round moved over it.
- **`the old startup flick is gone`**, and so are the hang's rail and wires.
- Carried over, changed only in how they reach a house (brought to the front with the
  keys first): resting brings the motifs after a moment — its "everything else goes out of
  focus" now reads the house beside the front one, since a house behind the axis is soft
  anyway — passing over the houses sets nothing off (swept back and forth across the
  houses that can be seen, since most are turned away), the motifs fade rather than
  vanish, every house's motifs draw, Qimu's staves, and pressing the front house steps the
  page back.

By hand: `npm run serve`, open `http://localhost:8123/categories/scent-descriptions.html`,
wheel, drag, press the numbers on the axis, rest on the front house.

## Known issues / TODO

- **On a phone the houses either side are cut by the window's edges**: the helix's reach
  is taken off the width, and on a phone that puts the neighbours half off the page. They
  are there to say there is more, not to be read.
- **The helix stops at the ends** rather than wrapping round from 09 to 01; the wheel and
  a drag give a little past either end and come back.
- **It does not remember where you were**: coming back to the page starts at 01.

## 2026-09-24, later still — the owner's first notes on it

> The houses page is pretty good, keep it that way. I want you to stabilize the thing in
> picture 1 so that it doesnt move when the hoiuse changes. I also want you to make the
> effects of hovering not blur stuff, and make it related to the whole page, so that they
> will look like additional effects from the page rather than hover elements (which they
> still are). i also want you to change the relative sizes of the displays, so that house
> presented in the middle of the page is as is, while the ones on the sides (+ and - 1) are
> sligjtly smaller. [...] i also want you to remove the descriptive text for the houses,
> only appearing if you hover it. Also fix the capitalisation on the house description
> please, it should be like a subtitle. make sure the effects of hovering happen BEHIND the
> perfume boxes please.

Everything else about the axis was kept as it was.

- **The way round stands still.** Its reading is a box of one size now (`width`,
  `height` on `.sheet-nav-at`) and the house's name one line, cut short with an ellipsis
  if it must be. "Qimu & Musicians" used to wrap on to a second line and push the button
  under it down, and a shorter name narrowed the column.
- **Resting blurs nothing.** The rest of the page used to go out of focus and dim while a
  house's motifs came up; now nothing does, and the house rested on only takes a deeper
  shadow. The `musing` class is still set — the tests time it — but no style hangs off it.
- **The motifs are the page's, and they stand behind the houses.** Their canvas is at
  z-index 0, under every house (1 to 4) and under the particles (2), on the squared ground
  itself; and they are no longer told to keep clear of the house rested on
  (`HouseMotifs.start` is handed no house), so they spread over the whole page. The one
  exception is **writing**: Tombstone's names are kept off every house that can be seen
  (the houses' boxes are handed over as `around`), because a name half hidden behind a
  picture has not been written.
- **The houses either side are a little smaller** — about three quarters of the front one
  rather than seven eighths. The scale now falls with the square of how far a house is
  turned away (`SIDE_FALL`); the front one is unchanged.
- **A house's line about itself shows only while it is pointed at**, the front one's
  included. For one round the front house showed its say all the time.
- **The says are capitalised as subtitles** (and since the end of the day in **title
  case**, *The House That Smells Like Trees* — see [the newer
  houses](2026-09-21-the-newer-houses.md)): *The house that smells like trees*, *The
  house that you have never heard of*, *The house I met at an expo* — and Ataraxia's is
  its new subtitle, *A gothic avante garde house*, in place of a line that was not the
  owner's. Pineward's and ADAR's own page subtitles, and their page titles, were given the
  same capital.

Tested in `tests/contact-sheet.spec.js`, each proved against its fault:
`resting on a house brings its motifs after a moment, behind the houses, blurring nothing`
(fails with the blur put back, and with the motifs over the houses), `the way round does
not move as the house changes` (fails with the reading's box unsized), `the houses either
side are a little smaller, and a description shows only on hover` (fails with the old
scale, and with the front house's say always shown). Twenty-six tests in the file now.

## 2026-09-24, last — the ring replaced by corners, and the axis emphasised

> In SD there is a spinning particle circle behind the house being shown, that is not a
> really aesthetic as it is hidden behind the square. I want you to replace it with
> something. I also want you to emphasize the middle part of the particles, the one around
> which the houses rotate.

- **The halo is gone** (`HALO_SPECKS` and its block in `draw()`). It was an ellipse of
  specks round the front house, and an ellipse round a box passes UNDER the box's corners —
  most of it was behind the picture, which is what the owner saw.
- **In its place, the corners**: four brackets of specks (`CORNER_*`), each two arms of
  `CORNER_LEG` px meeting in a larger speck, standing `CORNER_OUT` px outside the box made
  by the front house **and its label** — a registration mark stands outside what it
  registers, which is the site's own mark. Where that box is, is read off the page every
  frame (the label grows a line when the house is rested on) and eased, so the corners
  follow rather than jump. They breathe two pixels in and out, a light runs out along both
  arms of each on its own beat, and they draw themselves out from the corner as the house
  settles at the front. With reduced motion they simply stand.
- **The axis is heavier**: a line 1.8px wide at 0.8 of the ink rather than a 1px hairline
  at 0.45; a soft column of light `AXIS_GLOW` px either side of it; `AXIS_PULSES` streaks of
  light running down it at `PULSE_SPEED`; its ticks longer and darker; and its falling
  specks more of them (`AXIS_SPECKS`, 460 from 280) and gathered on the line rather than
  spread evenly five pixels either side. No pulses with reduced motion.

Tested in `tests/contact-sheet.spec.js`: `the front house is marked by four corners
standing clear of it and its label` (reads the particles' canvas 16px out from each
corner of the house-and-label box, against the same point 60px out; fails with the
corners taken out) and `the axis is drawn as a firm line with a light either side` (fails
with the old hairline, and with the light taken away). The Tombstone and Ataraxia changes
of the same round are in [the motifs' report](2026-09-23-the-chain-and-its-motifs.md). Thirty-two tests in the file now.


## 2026-09-24, night — the whole window, one thing after another, and an afterglow

> make the central division line of particles go all the way up, and make it so that the
> word houses/fragrances is more split apart and a little more emphasized (image 1) — the
> animation should be sequential, so you first have the central line, then the spiral of
> particles and only then the images. the images should load chronologically yet
> relatively quickly — make the central line go all the way down too (image 2) — I also
> dont want the houses to disappear midway through the white (2nd house to the left of
> current one, so if im hovering 5, then 3 is weirdly disappearing), image 3 & 4. do this
> for the bottom of the page too. — also make there to be a delay of the particles turning
> off after you hover them.

- **The houses have the whole window.** The sheet used to begin under the chrome (80px or
  so down, the `.sheet-stage`'s padding) and end 8px short of the foot, and it clips what
  is in it — so the axis stopped short at both ends, and a house two away from the front
  was **cut off along a line across the white** under the chrome: the owner's house 3 with
  5 at the front. The sheet is now pulled up to the window's top edge by a negative margin
  exactly as tall as that padding (`layout()` measures it with the margin taken off first)
  and runs to the foot; the page is still exactly the window and still does not scroll.
  **The helix is laid out where it always was**, in the room below the chrome (`roomTop`,
  `room` — what `H` used to be), so the front house has not moved a pixel; only what is
  drawn and what is clipped now reach the window's edges. The way round is placed at the
  middle of that room (`--room-middle`), where it stood, not the middle of the taller sheet.
- **A house is no longer faded as it nears the top or the foot.** It used to fade from
  partway down (`off`, from 42% of the room away from the middle), which on top of the cut
  made it look as if it were being rubbed out. It is let go of (hidden, untabbable) only
  once it and its label are wholly off the window.
- **The axis runs from the very top of the window to its very foot**, drawn out from the
  middle to both ends as the page arrives; its ticks, pulses and falling specks with it.
- **Houses and Fragrances are split by it.** The two words used to be one centred row at
  11px, and the axis ran up through the *F*. Each is now anchored to the middle of the
  window by its inner edge, `--split` (30px) from it — so the line passes exactly between
  them whatever either word's length — in capitals, at 12.5px and a heavier weight, with a
  short rule on each word's inner side pointing at the axis. The rules go on the
  Fragrances view, where there is no axis; the words stay put. On a phone each word has a
  ground of its own rather than one box round both (or the box would cover the axis), they
  are closer and at 11px, and the search is its mark alone, without the word, to leave
  them the room.
- **The arrival is one thing after another** (`AXIS_MS`, `HELIX_MS`, `EMERGE_STEP`): the
  axis, drawn out to both ends; then the helix, its strands **winding out** along the axis
  from the middle, up and down at once, with bright growing tips and the dust gathering
  round it (`helixShown`); and only then the houses, **01 first and the rest in number
  order**, 85ms apart. It used to bring the houses out while the axis was still being drawn
  and the helix came up with the axis. The whole takes about 2.4 seconds to the last house
  that can be seen.
- **The afterglow**: specks the pointer passes over stay lit for `GLOW_HOLD` (0.45s) after
  it has gone and then go out over `GLOW_FADE` (1.1s). The pointer leaves a **trail** —
  points along its way, 14px apart at least, each with the moment it was last there — and a
  speck is lit by the brightest of them as well as by the pointer itself. The pointer
  leaving the window leaves its last place on the trail, going out. None of it with reduced
  motion. **Read as the Houses view's particles**; the same was done on Ataraxia (see [the
  newer houses](2026-09-21-the-newer-houses.md)), since the owner did not say which page's.

Tested in `tests/contact-sheet.spec.js`, each run against the code before it and failing
there:

- **`first the axis, then the helix, and only then the houses, in order`** replaces *the
  houses come out of the axis one after another, nearest first*. Watched in the page a few
  times a second: when the axis is first drawn down the middle of the particles' canvas,
  when there is first ink anywhere off it, and when each house first shows — the helix at
  least 150ms after the axis, the first house at least 250ms after the helix, then 01, 02,
  03 in order, apart but inside 800ms. Fails against the old arrival on the first house
  coming before the helix.
- **`the axis runs from the top of the window to its foot, between Houses and Fragrances`**
  — the axis in over 90% of the top 40 rows of the window and of the bottom 40; Houses left
  of it, Fragrances right of it, evenly; the words over 11px. Fails against the old page on
  the top.
- **`a house two away from the front runs off the window's edge rather than stopping short
  of it`** — with 05 at the front, the sheet begins at the window's top and ends at its
  foot, house 3 is seen and runs up to within 40px of the top, house 7 is seen and runs off
  the foot, and the page does not scroll. Fails against the old sheet, which began at 81px.
- **`the specks the pointer passes over stay lit a moment after it has gone`** — ink round
  a stretch of the axis with nothing in front of it: brighter with the pointer there, still
  brighter 150ms after it has moved to a far corner, and back down after 2.6 seconds.
  **The far corner has to be inside the window**: the first version sent the pointer to
  (4, 890) on a 720px window, the page never heard it leave, and the test passed against
  the old code because the old code still thought the pointer was there.

Thirty-five tests in the file now.

## 2026-09-25, night — drawn through ADAR's wells, and a way over to the Fragrances

Two small things in `contact-sheet.js`, each belonging to a larger change written up elsewhere:

- **Drawn through a field.** While ADAR is rested on, its motifs are black holes that pull on
  everything they touch ([the motifs' report](2026-09-23-the-chain-and-its-motifs.md)).
  `draw()` reads `window.HouseMotifs.bend()` once a frame (`bend`, declared up with `pos` because
  `place()` runs before the particles are set up), and while it is not null: every `speck()` is
  moved, scaled and faded by it; the axis line and its pulses are drawn as paths through it and
  its ticks moved by it; and in `place()` every house **but the one rested on** leans 70% of the
  way it is pulled, turns half as far, and shrinks no further than half — as do the numbers on
  the axis. With no well, nothing is different from before.
- **The fragrances →** under the way round: a button carrying `data-view-go="fragrances"`, which
  `views.js` takes as a press of the Fragrances button — the Houses' end of the crossing's
  connection ([the fragrance line](2026-09-25-the-fragrance-line.md)). Hidden below 700px.


## 2026-09-25, later that night — the houses beside the front stepped back, the axis quieter, and spokes

The owner, with a screenshot of 03 at the front: *"I would like you to fade and make smaller
the non-selected house (not hovered, but the one on which the page rests) … I would want the
house 01 and 02 to be slightly less prioritized ont he page (as well sa 04 and 05 … de
emphasize the blacj line in the middle in SD. (especially with the shadow around it)
Additionally, I want you to connect the fragrances to the spiral in a 3d way, so that the
conenctions of the fragrances to the central line will move in a 3rd dimension along
withthe fragrances. do this while slightly emphasizing the spiral line guiding the houses.
make the connecting line particular (houses to central line)"*. The house "on which the page
rests" is the **front**; the ones to step back are all the others. All of it is in
`contact-sheet.js`.

- **The others smaller and fainter.** `SMALLEST` 0.46 → 0.4 and `SIDE_FALL` 2 → 2.6, so the
  houses either side of the front are about two thirds of its size rather than three
  quarters; and a new pair, `SIDE_FADE` (0.25) and `SIDE_FADE_FALL` (1.8), fade them on a
  curve — `seen = come × (SIDE_FADE + (1 − SIDE_FADE) × ((z + 1) / 2)^1.8)` — so the ones
  either side are at about two thirds of the front's strength and the ones beyond at under a
  third. It was a straight `0.3 + 0.7 × …`, which left the neighbours at four fifths. **The
  one rested on under the pointer is untouched** (`.hot` still brings it forward), because
  the owner said *not hovered*.
- **The axis quieter.** Everything it was given on 2026-09-24 ("emphasised") is taken
  most of the way back, and it is named now rather than written inline: the light either side
  `AXIS_GLOW` 22 → 12px wide and `AXIS_GLOW_LIT` 0.1 → 0.035 strong (the *shadow* the owner
  named — it was the glow, which on white reads as a grey shadow); the line `AXIS_LINE`
  1.8 → 1.2px at `AXIS_LINE_LIT` 0.8 → 0.55; the pulses running down it `PULSE_LIT`
  0.75 → 0.35 and 3.5 → 2px wide; the specks falling down it at 0.55 of what they were
  drawn at rather than 0.85.
- **The spokes.** The **tethers** — a flat line of thirty specks from the axis straight out
  to the house, lying on the window — are gone, and in their place every house has a
  **spoke**: a loose tube of `SPOKE_PARTS` (130) particles standing in the helix's own
  space, from the axis to where its house stands. Each particle has a place along the spoke
  (`q`), a distance off its line up to `SPOKE_WIDTH` (5px) — thin at the axis, fullest part
  way out — and an angle round it, and is turned into the window by the same angle the house
  is (`a = d × TURN`): so as the helix turns, the spokes turn with it, foreshortening as a
  house goes round the back and lengthening as it comes to the front. Nearer particles are
  larger and stronger, the particles flow slowly outward along the spoke, the tube twists
  about itself (`SPOKE_TWIST`, a quarter of a radian a second), and the old tethers' pulse
  still runs outward along it. It is drawn in **two passes** (`spokesPass(t, behind)`): the
  half of every spoke standing behind the axis before the axis is drawn, and the half in
  front after, so a spoke going round the back really goes behind the line. The particles
  are seeded, so the spokes are the same every visit.
- **The helix a little stronger.** A strand speck `1.1 + depth × 1.8` rather than
  `0.9 + depth × 1.6`, at `0.22 + 0.72 × depth` rather than `0.16 + 0.62 × depth`, the second
  strand at 0.7 of the first rather than 0.6.

With reduced motion the spokes stand still, untwisted, the pulse off.

**How to test it** — in `tests/contact-sheet.spec.js`, forty-three tests now:

- **`the houses either side are smaller and fainter, and a description shows only on hover`**
  (was *a little smaller*) — the neighbours under 0.72 of the front's width and over 0.6;
  the front's opacity over 0.97 and the neighbours' under 0.75 and over 0.5.
- **`the axis is a quiet line, with hardly any light either side`** (replaces *the axis is
  drawn as a firm line with a light either side*) — read off a stretch of the axis with
  nothing in front of it: the line is still there (over 85% of rows carry ink over 90 of
  255), but hardly any of it is dark (under a fifth over 200), and beside it there is almost
  nothing (under 0.3 of the rows lit). Fails against the old axis on every count.
- **`each house is joined to the axis by a spoke of particles standing in the helix's
  depth`** — between the axis and a house, the ink stands on five rows or more (a tube, not
  a line of specks one row high), and there is more of it towards the house than towards
  the axis. Fails against the tethers, which were one row.
