# Dependencies
node_modules/

# Build output
dist/
build/
out/
.cache/

# Environment variables / secrets
.env
.env.*
!.env.example

# Test output (regenerated every run — never worth committing)
test-results/
playwright-report/
blob-report/
.playwright/

# npm logs
npm-debug.log*

# Scratch scripts used to drive a browser at the site while working on
# it (screenshots, one-off measurements). One of these was committed by
# accident on 2026-09-21 by a `git add -A`; it is not part of the site.
look-tmp.js
*-tmp.js
